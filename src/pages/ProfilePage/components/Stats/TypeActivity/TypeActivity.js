import PropTypes from 'prop-types'
import { Card } from '../../../../../components'

export default function TypeActivity({ performance /*: { kind, data} */ }) {
  return (
    <Card type="cardMedium" color="cardDark">
      {/* Graphique radar */}
    </Card>
  )
}

TypeActivity.propTypes = {

}
